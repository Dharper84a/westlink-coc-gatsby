import { colors } from "./theme"

export const darkStyles = {
  text: {
    color: colors.darkBlack,
  },
  background: {
    siteHeader: colors.white,
    siteFooter: colors.darkBlack,
  },
  button: {
    textColor: colors.darkBlack,
    iconColor: colors.green,
    solid: {
      bg: colors.veryLightGray,
    }
  }
}